#include <stdio.h>

int main() {
    int length, width, height, volume;
    length = 5;
    width = 3;
    height = 2;
    volume = length * width * height;
    printf("Volume of Rectangular Prism %d", volume);
    return 0;
}
